import { useState, useEffect } from 'react';import { Link } from 'react-router-dom';import AdminSidebar from '@/components/admin/AdminSidebar';import { api } from '@/api/apiClient';import {  CalendarDays, Users, Wallet, Scissors, TrendingUp, ChevronDown,  MoreHorizontal, Plus, Eye, Pencil, Trash2, Clock, Bell,  CalendarPlus, Ban, UserPlus, Stethoscope, Tag, Send, CheckCircle2,  CircleDollarSign} from 'lucide-react';const fmt = (v) => `R$ ${Number(v).toFixed(2).replace('.', ',')}`;const quickActions = [
  { icon: CalendarPlus, label: 'Novo Agendamento', href: '/agendamento' },
  { icon: Scissors, label: 'Cadastrar Servico', href: '/dashboard/servicos' },
  { icon: Stethoscope, label: 'Cadastrar Profissional', href: '/dashboard/profissionais' },
];export default function Admin() {
  const [appointments, setAppointments] = useState([]);
  const [clients, setClients] = useState({ total: 0 });
  const [services, setServices] = useState([]);
  const [fDate, setFDate] = useState('');
  const [fStatus, setFStatus] = useState('');
  const [fService, setFService] = useState('');

  useEffect(() => {
    Promise.all([
      api.Appointment.list('-created_date', 200).catch(() => []),
      api.Client.list({ limit: 1 }).catch(() => ({ total: 0 })),
      api.Service.list().catch(() => []),
    ]).then(([appts, cl, svc]) => {
      setAppointments(appts);
      setClients(cl);
      setServices(svc);
    });
  }, []);

  const list = appointments.length ? appointments : [];
  const filteredTable = list.filter((a) =>
    (!fDate || a.date === fDate) &&
    (!fStatus || a.status === fStatus) &&
    (!fService || (a.service_name || '').toLowerCase().includes(fService.toLowerCase()))
  );

  const today = new Date().toISOString().split('T')[0];
  const todayAppts = list.filter((a) => a.date === today);
  const completed = list.filter((a) => a.status === 'completed');
  const revenue = completed.reduce((s, a) => s + Number(a.total_price || 0), 0);

  const metrics = [
    { label: 'Agendamentos Hoje', value: String(todayAppts.length), icon: CalendarDays },
    { label: 'Clientes Ativos', value: String(clients.total || 0), icon: Users },
    { label: 'Receita Total', value: fmt(revenue), icon: Wallet },
    { label: 'Servicos Realizados', value: String(completed.length), icon: Scissors },
  ];  return (    <div className="flex bg-[#fcf9f6] min-h-screen">      <AdminSidebar />      <div className="flex-1 min-w-0">        {/* Header */}        <header className="bg-white border-b border-[#E0DCD6] px-8 py-4 flex items-center justify-between sticky top-0 z-10">          <div>            <h1 className="font-display font-bold text-xl text-[#2b2622]">Dashboard</h1>            <p className="text-xs text-[#2b2622]/50">Visão geral da clínica</p>          </div>          <div className="flex items-center gap-4">            <button className="flex items-center gap-2 px-4 py-2 border border-[#E0DCD6] rounded-lg text-sm text-[#2b2622]/70 hover:bg-[#F5EFE6]">              Últimos 7 dias <ChevronDown size={15} />            </button>            <div className="flex items-center gap-2.5">              <div className="w-9 h-9 rounded-full bg-[#B67D35] flex items-center justify-center text-white font-display font-semibold text-sm">A</div>              <div className="hidden sm:block">                <p className="text-sm font-medium text-[#2b2622]">Admin</p>                <p className="text-[11px] text-[#2b2622]/50">Administrador</p>              </div>            </div>          </div>        </header>        <div className="p-8 space-y-6">          {/* Metrics */}          <div className="grid grid-cols-2 lg:grid-cols-4 gap-5">            {metrics.map((m) => (              <div key={m.label} className="bg-white border border-[#E0DCD6] rounded-2xl p-5">                <div className="flex items-start justify-between">                  <div className="w-10 h-10 rounded-xl bg-[#B67D35]/10 flex items-center justify-center">                    <m.icon size={18} className="text-[#B67D35]" />                  </div>                  <span className="flex items-center gap-1 text-xs text-emerald-600 font-medium">                    <TrendingUp size={13} /> {m.delta}                  </span>                </div>                <p className="font-display font-bold text-2xl text-[#2b2622] mt-4">{m.value}</p>                <p className="text-xs text-[#2b2622]/50 mt-1">{m.label}</p>              </div>            ))}          </div>          <div className="grid lg:grid-cols-3 gap-6">            {/* Recent appointments */}            <div className="lg:col-span-2 bg-white border border-[#E0DCD6] rounded-2xl overflow-hidden">              <div className="px-5 py-4 border-b border-[#E0DCD6] flex items-center justify-between">                <h2 className="font-display font-semibold text-[#2b2622]">Agendamentos Recentes</h2>                <Link to="/dashboard/agendamentos" className="text-xs text-[#B67D35] font-medium hover:underline">Ver todos</Link>              </div>              <div className="px-5 py-3 border-b border-[#E0DCD6] flex flex-wrap gap-3 items-center">                <input type="date" value={fDate} onChange={(e) => setFDate(e.target.value)} className="px-3 py-1.5 text-sm border border-[#E0DCD6] rounded-lg text-[#2b2622] focus:outline-none focus:border-[#B67D35]" />                <select value={fStatus} onChange={(e) => setFStatus(e.target.value)} className="px-3 py-1.5 text-sm border border-[#E0DCD6] rounded-lg text-[#2b2622] focus:outline-none focus:border-[#B67D35]">                  <option value="">Todos os status</option>                  <option value="confirmed">Confirmado</option>                  <option value="pending">Pendente</option>                  <option value="completed">Concluído</option>                  <option value="cancelled">Cancelado</option>                </select>                <input type="text" value={fService} onChange={(e) => setFService(e.target.value)} placeholder="Buscar serviço..." className="px-3 py-1.5 text-sm border border-[#E0DCD6] rounded-lg flex-1 min-w-[140px] text-[#2b2622] placeholder:text-[#2b2622]/30 focus:outline-none focus:border-[#B67D35]" />              </div>              <div className="overflow-x-auto">                <table className="w-full text-sm">                  <thead>                    <tr className="bg-[#fcf9f6] text-[10px] uppercase tracking-widest text-[#2b2622]/50">                      <th className="text-left font-medium px-5 py-3">Data</th>                      <th className="text-left font-medium px-3 py-3">Serviço</th>                      <th className="text-left font-medium px-3 py-3">Cliente</th>                      <th className="text-left font-medium px-3 py-3">Status</th>                      <th className="text-left font-medium px-3 py-3">Pagamento</th>                      <th className="px-3 py-3"></th>                    </tr>                  </thead>                  <tbody className="divide-y divide-[#E0DCD6]">                    {filteredTable.slice(0, 6).map((a, i) => (                      <tr key={i} className="hover:bg-[#fcf9f6] transition-colors">                        <td className="px-5 py-3.5">                          <p className="text-[#2b2622] font-medium">{a.time}</p>                          <p className="text-[11px] text-[#2b2622]/50">{new Date(a.date + 'T00:00').toLocaleDateString('pt-BR', { day: '2-digit', month: 'short' })}</p>                        </td>                        <td className="px-3 py-3.5 text-[#2b2622]/80">{a.service_name}</td>                        <td className="px-3 py-3.5 text-[#2b2622]/80">{a.client_name}</td>                        <td className="px-3 py-3.5"><StatusBadge status={a.status} /></td>                        <td className="px-3 py-3.5"><PayBadge method={a.payment_method} /></td>                        <td className="px-3 py-3.5"><button className="text-[#2b2622]/40 hover:text-[#2b2622]"><MoreHorizontal size={16} /></button></td>                      </tr>                    ))}                  </tbody>                </table>              </div>            </div>            {/* Upcoming */}            <div className="bg-white border border-[#E0DCD6] rounded-2xl p-5">              <h2 className="font-display font-semibold text-[#2b2622] mb-4">Próximos Agendamentos</h2>              <div className="space-y-3">                {list.slice(0, 4).map((a, i) => (                  <div key={i} className="flex items-center gap-3 p-3 rounded-xl bg-[#fcf9f6]">                    <div className="w-10 h-10 rounded-full bg-[#B67D35]/10 flex items-center justify-center shrink-0">                      <Clock size={16} className="text-[#B67D35]" />                    </div>                    <div className="min-w-0 flex-1">                      <p className="text-sm font-medium text-[#2b2622] truncate">{a.service_name}</p>                      <p className="text-xs text-[#2b2622]/50">{a.client_name} • {a.time}</p>                    </div>                    <StatusBadge status={a.status} />                  </div>                ))}              </div>            </div>          </div>          <div className="grid lg:grid-cols-2 gap-6">            {/* Calendar */}            <div className="bg-white border border-[#E0DCD6] rounded-2xl p-5">              <h2 className="font-display font-semibold text-[#2b2622] mb-4">Calendário Geral</h2>              <MiniCalendar />              <div className="flex flex-wrap gap-4 mt-4 pt-4 border-t border-[#E0DCD6]">                <Legend color="bg-emerald-500" label="Dia com agendamentos" />                <Legend color="bg-gray-400" label="Poucos agendamentos" />                <Legend color="bg-red-500" label="Bloqueado / Feriado" />              </div>            </div>            {/* Time slots */}            <div className="bg-white border border-[#E0DCD6] rounded-2xl p-5">              <div className="flex items-center justify-between mb-4">                <h2 className="font-display font-semibold text-[#2b2622]">Gerenciar Horários</h2>                <button className="flex items-center gap-1 text-xs text-[#B67D35] font-medium hover:underline">                  <Plus size={13} /> Adicionar / Bloquear                </button>              </div>              <div className="grid grid-cols-4 gap-2">                {['08:00','09:00','10:00','11:00','12:00','13:00','14:00','15:00','16:00','17:00','18:00','19:00'].map((slot) => {                  const state = slot === '14:00' ? 'booked' : slot === '11:00' ? 'blocked' : 'free';                  return (                    <div                      key={slot}                      className={`py-2.5 rounded-lg text-center text-sm font-medium ${                        state === 'booked' ? 'bg-[#B67D35] text-white' : state === 'blocked' ? 'bg-red-50 text-red-400 line-through' : 'bg-[#F5EFE6] text-[#2b2622]/70'                      }`}                    >                      {slot}                    </div>                  );                })}              </div>              <div className="flex flex-wrap gap-4 mt-4 pt-4 border-t border-[#E0DCD6]">                <Legend color="bg-[#F5EFE6]" label="Disponível" />                <Legend color="bg-[#B67D35]" label="Agendado" />                <Legend color="bg-red-500" label="Bloqueado" />              </div>            </div>          </div>          <div className="grid lg:grid-cols-3 gap-6">            {/* Quick actions */}            <div className="bg-white border border-[#E0DCD6] rounded-2xl p-5">              <h2 className="font-display font-semibold text-[#2b2622] mb-4">Ações Rápidas</h2>              <div className="grid grid-cols-3 gap-3">                {quickActions.map((a) => (                  <button key={a.label} className="aspect-square rounded-xl border border-[#E0DCD6] hover:border-[#B67D35] hover:bg-[#B67D35]/5 flex flex-col items-center justify-center gap-2 p-2 transition-all">                    <a.icon size={18} className="text-[#B67D35]" />                    <span className="text-[10px] text-center text-[#2b2622]/70 leading-tight">{a.label}</span>                  </button>                ))}              </div>            </div>            {/* Recent activity */}            <div className="lg:col-span-2 bg-white border border-[#E0DCD6] rounded-2xl p-5">              <h2 className="font-display font-semibold text-[#2b2622] mb-4">Atividade Recente</h2>              <div className="space-y-4">                {[                  { icon: CheckCircle2, text: 'Agendamento confirmado', detail: 'Limpeza de Pele — Mariana Costa', time: 'Hoje, 10:24' },                  { icon: CircleDollarSign, text: 'Pagamento recebido', detail: 'R$ 142,50 — PIX', time: 'Hoje, 09:15' },                  { icon: Users, text: 'Novo cliente cadastrado', detail: 'Patrícia Almeida', time: 'Ontem, 18:40' },                  { icon: Send, text: 'Notificação enviada', detail: 'Lembrete de agendamento', time: 'Ontem, 14:00' },                ].map((act, i) => (                  <div key={i} className="flex items-center gap-3">                    <div className="w-9 h-9 rounded-full bg-[#B67D35]/10 flex items-center justify-center shrink-0">                      <act.icon size={15} className="text-[#B67D35]" />                    </div>                    <div className="flex-1 min-w-0">                      <p className="text-sm text-[#2b2622]">{act.text}</p>                      <p className="text-xs text-[#2b2622]/50">{act.detail}</p>                    </div>                    <span className="text-xs text-[#2b2622]/40 shrink-0">{act.time}</span>                  </div>                ))}              </div>            </div>          </div>                    {/* Recent users */}
          <RecentUsers />          <footer className="text-center text-xs text-[#2b2622]/40 pt-4">            © 2025 Beleza & Bem-Estar. Todos os direitos reservados.          </footer>        </div>      </div>    </div>  );}function StatusBadge({ status }) {  const map = {    confirmed: { cls: 'bg-emerald-50 text-emerald-700', label: 'Confirmado' },    pending: { cls: 'bg-orange-50 text-orange-700', label: 'Pendente' },    cancelled: { cls: 'bg-red-50 text-red-700', label: 'Cancelado' },    completed: { cls: 'bg-blue-50 text-blue-700', label: 'Concluído' },  };  const s = map[status] || map.pending;  return <span className={`px-2.5 py-1 rounded-full text-xs font-medium ${s.cls}`}>{s.label}</span>;}function PayBadge({ method }) {  const paid = method === 'pix' || method === 'credit';  return (    <span className={`px-2.5 py-1 rounded-full text-xs font-medium ${paid ? 'bg-emerald-50 text-emerald-700' : 'bg-gray-100 text-gray-600'}`}>      {paid ? 'Pago' : 'Pendente'} ({method === 'pix' ? 'PIX' : method === 'credit' ? 'Crédito' : 'Débito'})    </span>  );}function Legend({ color, label }) {  return (    <div className="flex items-center gap-2">      <span className={`w-2.5 h-2.5 rounded-full ${color}`} />      <span className="text-xs text-[#2b2622]/60">{label}</span>    </div>  );}function MiniCalendar() {  const today = new Date();  const [view, setView] = useState(new Date(today.getFullYear(), today.getMonth(), 1));  const year = view.getFullYear();  const month = view.getMonth();  const firstDay = new Date(year, month, 1).getDay();  const days = new Date(year, month + 1, 0).getDate();  const cells = [];  for (let i = 0; i < firstDay; i++) cells.push(null);  for (let d = 1; d <= days; d++) cells.push(d);  const booked = new Set([3, 5, 12, 15, 20, 22, 28]);  const blocked = new Set([7, 25]);  return (    <div>      <div className="flex items-center justify-between mb-3">        <span className="font-display font-semibold text-[#2b2622] text-sm">          {['Janeiro','Fevereiro','Março','Abril','Maio','Junho','Julho','Agosto','Setembro','Outubro','Novembro','Dezembro'][month]} {year}        </span>        <div className="flex gap-1">          <button onClick={() => setView(new Date(year, month - 1, 1))} className="w-7 h-7 rounded-lg hover:bg-[#F5EFE6] text-[#2b2622]/60 text-sm">‹</button>          <button onClick={() => setView(new Date(year, month + 1, 1))} className="w-7 h-7 rounded-lg hover:bg-[#F5EFE6] text-[#2b2622]/60 text-sm">›</button>        </div>      </div>      <div className="grid grid-cols-7 gap-1 text-center text-[10px] text-[#2b2622]/40 mb-1">        {['D','S','T','Q','Q','S','S'].map((w, i) => <div key={i}>{w}</div>)}      </div>      <div className="grid grid-cols-7 gap-1">        {cells.map((d, i) => {          if (!d) return <div key={i} />;          const isToday = d === today.getDate() && month === today.getMonth();          const isBooked = booked.has(d);          const isBlocked = blocked.has(d);          return (            <div              key={i}              className={`h-8 rounded-lg text-xs flex items-center justify-center relative ${                isToday ? 'bg-[#B67D35] text-white font-semibold' : isBlocked ? 'text-red-400' : 'text-[#2b2622]/70 hover:bg-[#F5EFE6]'              }`}            >              {d}              {!isToday && isBooked && <span className="absolute bottom-0.5 w-1 h-1 rounded-full bg-emerald-500" />}              {!isToday && isBlocked && <span className="absolute bottom-0.5 w-1 h-1 rounded-full bg-red-500" />}            </div>                    );
        })}
      </div>
    </div>
  );
}

function RecentUsers() {
  const [users, setUsers] = useState([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    api.Client.list({ limit: 5 })
      .then((data) => setUsers(data.clientes || []))
      .catch(() => setUsers([]))
      .finally(() => setLoading(false));
  }, []);

  return (
    <div className="bg-white border border-[#E0DCD6] rounded-2xl overflow-hidden">
      <div className="px-5 py-4 border-b border-[#E0DCD6]">
        <h2 className="font-display font-semibold text-[#2b2622]">Clientes Recentes</h2>
      </div>
      <div className="overflow-x-auto">
        <table className="w-full text-sm">
          <thead>
            <tr className="bg-[#fcf9f6] text-[10px] uppercase tracking-widest text-[#2b2622]/50">
              <th className="text-left font-medium px-5 py-3">Nome</th>
              <th className="text-left font-medium px-3 py-3">E-mail</th>
              <th className="text-left font-medium px-3 py-3">Agendamentos</th>
              <th className="text-left font-medium px-3 py-3">Pontos</th>
            </tr>
          </thead>
          <tbody className="divide-y divide-[#E0DCD6]">
            {loading ? (
              <tr><td colSpan={4} className="px-5 py-8 text-center text-[#2b2622]/40">Carregando...</td></tr>
            ) : users.length === 0 ? (
              <tr><td colSpan={4} className="px-5 py-8 text-center text-[#2b2622]/40">Nenhum cliente encontrado</td></tr>
            ) : users.map((u) => (
              <tr key={u.id} className="hover:bg-[#fcf9f6] transition-colors">
                <td className="px-5 py-3.5">
                  <div className="flex items-center gap-2.5">
                    <div className="w-8 h-8 rounded-full bg-[#B67D35]/15 flex items-center justify-center text-[#B67D35] font-display font-semibold text-xs">
                      {(u.nome || '').split(' ').map((n) => n[0]).join('').slice(0, 2)}
                    </div>
                    <span className="text-[#2b2622] font-medium">{u.nome}</span>
                  </div>
                </td>
                <td className="px-3 py-3.5 text-[#2b2622]/70">{u.email}</td>
                <td className="px-3 py-3.5 text-[#2b2622]/70">{u.totalAgendamentos || 0}</td>
                <td className="px-3 py-3.5 text-[#2b2622]/70">{u.pontosFidelidade || 0}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  );
}