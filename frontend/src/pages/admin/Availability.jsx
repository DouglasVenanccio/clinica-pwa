import { useState, useEffect } from 'react';import { Link } from 'react-router-dom';import AdminSidebar from '@/components/admin/AdminSidebar';import { api } from '@/api/apiClient';import { ArrowLeft, Save, Loader2, CalendarOff } from 'lucide-react';const DAYS = [  { key: 'slots_mon', label: 'Segunda' },  { key: 'slots_tue', label: 'Terça' },  { key: 'slots_wed', label: 'Quarta' },  { key: 'slots_thu', label: 'Quinta' },  { key: 'slots_fri', label: 'Sexta' },  { key: 'slots_sat', label: 'Sábado' },  { key: 'slots_sun', label: 'Domingo' },];const DEFAULT_SLOTS = '08:00,09:00,10:00,11:00,13:00,14:00,15:00,16:00,17:00,18:00,19:00';export default function Availability() {  const [pros, setPros] = useState([]);  const [schedules, setSchedules] = useState({});  const [selectedPro, setSelectedPro] = useState(null);  const [loading, setLoading] = useState(true);  const [saving, setSaving] = useState(false);  useEffect(() => {    api.Professional.list()      .then((data) => {        setPros(data || []);        if (data && data.length) setSelectedPro(data[0]);      })      .catch(() => setPros([]))      .finally(() => setLoading(false));  }, []);  useEffect(() => {    if (!selectedPro) return;    api.Schedule.filter({ professional_name: selectedPro.name })      .then((data) => {        if (data && data.length) {          setSchedules((prev) => ({ ...prev, [selectedPro.name]: data[0] }));        } else {          setSchedules((prev) => ({ ...prev, [selectedPro.name]: null }));        }      })      .catch(() => {});  }, [selectedPro]);  const current = selectedPro ? schedules[selectedPro.name] : null;  const getSlot = (key) => {    if (!current) return key === 'slots_sat' ? '08:00,09:00,10:00,11:00' : key === 'slots_sun' ? '' : DEFAULT_SLOTS;    return current[key] || '';  };  const setSlot = (key, value) => {    setSchedules((prev) => {      const existing = prev[selectedPro.name];      const updated = { ...(existing || { professional_name: selectedPro.name }), [key]: value };      return { ...prev, [selectedPro.name]: updated };    });  };  const handleSave = async () => {    if (!selectedPro || !schedules[selectedPro.name]) return;    setSaving(true);    const data = schedules[selectedPro.name];    try {      if (current?.id) {        await api.Schedule.update(current.id, data);      } else {        const created = await api.Schedule.create(data);        setSchedules((prev) => ({ ...prev, [selectedPro.name]: created }));      }    } catch (e) { console.error(e); }    finally { setSaving(false); }  };  return (    <div className="flex bg-[#fcf9f6] min-h-screen">      <AdminSidebar />      <div className="flex-1 min-w-0">        <header className="bg-white border-b border-[#E0DCD6] px-8 py-4 flex items-center justify-between sticky top-0 z-10">          <div className="flex items-center gap-3">            <Link to="/dashboard" className="w-9 h-9 rounded-full hover:bg-[#F5EFE6] flex items-center justify-center text-[#2b2622]/60"><ArrowLeft size={18} /></Link>            <div>              <h1 className="font-display font-bold text-xl text-[#2b2622]">Disponibilidade</h1>              <p className="text-xs text-[#2b2622]/50">Horários e folgas por profissional</p>            </div>          </div>          {selectedPro && (            <button onClick={handleSave} disabled={saving} className="flex items-center gap-2 px-4 py-2 bg-[#B67D35] hover:bg-[#9c6829] disabled:opacity-50 text-white rounded-lg text-sm font-medium transition-colors">              {saving ? <Loader2 className="animate-spin" size={16} /> : <Save size={16} />} Salvar            </button>          )}        </header>        <div className="p-8">          {loading ? (            <div className="flex justify-center py-16"><Loader2 className="animate-spin text-[#B67D35]" size={24} /></div>          ) : pros.length === 0 ? (            <div className="text-center py-16">              <CalendarOff className="mx-auto mb-3 text-[#2b2622]/20" size={32} />              <p className="text-sm text-[#2b2622]/50">Cadastre profissionais primeiro para gerenciar disponibilidade.</p>            </div>          ) : (            <div className="grid lg:grid-cols-4 gap-6">              <div className="space-y-1">                <p className="text-xs uppercase tracking-widest text-[#2b2622]/50 font-medium mb-3 px-2">Profissionais</p>                {pros.map((p) => (                  <button                    key={p.id}                    onClick={() => setSelectedPro(p)}                    className={`w-full text-left px-4 py-3 rounded-xl text-sm transition-colors ${selectedPro?.id === p.id ? 'bg-[#B67D35] text-white font-medium' : 'bg-white border border-[#E0DCD6] text-[#2b2622] hover:bg-[#F5EFE6]'}`}                  >                    {p.name}                  </button>                ))}              </div>              <div className="lg:col-span-3 bg-white border border-[#E0DCD6] rounded-2xl p-6">                <h2 className="font-display font-semibold text-[#2b2622] mb-1">Horários de {selectedPro?.name}</h2>                <p className="text-xs text-[#2b2622]/50 mb-5">Informe os horários separados por vírgula (ex: 08:00,09:00,10:00). Deixe vazio para folga.</p>                <div className="space-y-4">                  {DAYS.map((day) => (                    <div key={day.key} className="grid grid-cols-[100px_1fr] gap-4 items-center">                      <span className="text-sm font-medium text-[#2b2622]/70">{day.label}</span>                      <input                        type="text"                        value={getSlot(day.key)}                        onChange={(e) => setSlot(day.key, e.target.value)}                        placeholder="Folga"                        className="w-full px-4 py-2.5 bg-white border border-[#E0DCD6] rounded-xl text-sm text-[#2b2622] placeholder:text-[#2b2622]/30 focus:outline-none focus:border-[#B67D35] font-mono"                      />                    </div>                  ))}                  <div className="grid grid-cols-[100px_1fr] gap-4 items-center pt-4 border-t border-[#E0DCD6]">                    <span className="text-sm font-medium text-[#2b2622]/70">Folgas</span>                    <input                      type="text"                      value={current?.days_off || ''}                      onChange={(e) => setSlot('days_off', e.target.value)}                      placeholder="2026-08-15,2026-12-25"                      className="w-full px-4 py-2.5 bg-white border border-[#E0DCD6] rounded-xl text-sm text-[#2b2622] placeholder:text-[#2b2622]/30 focus:outline-none focus:border-[#B67D35] font-mono"                    />                  </div>                </div>              </div>            </div>          )}        </div>      </div>    </div>  );}