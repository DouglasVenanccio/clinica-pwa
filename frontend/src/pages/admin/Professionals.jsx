import { useState, useEffect } from 'react';import { Link } from 'react-router-dom';import AdminSidebar from '@/components/admin/AdminSidebar';import { api } from '@/api/apiClient';import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter } from '@/components/ui/dialog';import { Button } from '@/components/ui/button';import { ArrowLeft, Plus, Pencil, Trash2, Star, Loader2, Users } from 'lucide-react';export default function Professionals() {  const [items, setItems] = useState([]);  const [loading, setLoading] = useState(true);  const [open, setOpen] = useState(false);  const [editing, setEditing] = useState(null);  const [form, setForm] = useState({ name: '', specialty: '', rating: 5, reviews: 0, initials: '' });  const load = async () => {    setLoading(true);    try {      const data = await api.Professional.list('-created_date', 100);      setItems(data || []);    } catch { setItems([]); }    finally { setLoading(false); }  };  useEffect(() => { load(); }, []);  const getInitials = (name) => name.split(' ').map((n) => n[0]).join('').slice(0, 2).toUpperCase();  const openNew = () => { setEditing(null); setForm({ name: '', specialty: '', rating: 5, reviews: 0, initials: '' }); setOpen(true); };  const openEdit = (p) => { setEditing(p); setForm({ name: p.name, specialty: p.specialty || '', rating: p.rating || 5, reviews: p.reviews || 0, initials: p.initials || '' }); setOpen(true); };  const handleSave = async () => {    if (!form.name) return;    const data = { ...form, initials: form.initials || getInitials(form.name), rating: Number(form.rating), reviews: Number(form.reviews) };    if (editing) {      await api.Professional.update(editing.id, data);    } else {      await api.Professional.create(data);    }    setOpen(false);    load();  };  const handleDelete = async (p) => {    if (!window.confirm(`Excluir a profissional "${p.name}"?`)) return;    await api.Professional.delete(p.id);    load();  };  return (    <div className="flex bg-[#fcf9f6] min-h-screen">      <AdminSidebar />      <div className="flex-1 min-w-0">        <header className="bg-white border-b border-[#E0DCD6] px-8 py-4 flex items-center justify-between sticky top-0 z-10">          <div className="flex items-center gap-3">            <Link to="/admin" className="w-9 h-9 rounded-full hover:bg-[#F5EFE6] flex items-center justify-center text-[#2b2622]/60"><ArrowLeft size={18} /></Link>            <div>              <h1 className="font-display font-bold text-xl text-[#2b2622]">Profissionais</h1>              <p className="text-xs text-[#2b2622]/50">{items.length} cadastradas</p>            </div>          </div>          <button onClick={openNew} className="flex items-center gap-2 px-4 py-2 bg-[#B67D35] hover:bg-[#9c6829] text-white rounded-lg text-sm font-medium transition-colors">            <Plus size={16} /> Nova Profissional          </button>        </header>        <div className="p-8">          <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-5">            {loading ? (              <div className="col-span-full flex justify-center py-16"><Loader2 className="animate-spin text-[#B67D35]" size={24} /></div>            ) : items.length === 0 ? (              <div className="col-span-full text-center py-16">                <Users className="mx-auto mb-3 text-[#2b2622]/20" size={32} />                <p className="text-sm text-[#2b2622]/50">Nenhuma profissional cadastrada ainda.</p>              </div>            ) : items.map((p) => (              <div key={p.id} className="bg-white border border-[#E0DCD6] rounded-2xl p-5">                <div className="flex items-start justify-between mb-3">                  <div className="w-12 h-12 rounded-full bg-gradient-to-br from-[#B67D35] to-[#9c6829] flex items-center justify-center text-white font-display font-bold">                    {p.initials || getInitials(p.name)}                  </div>                  <div className="flex gap-1">                    <button onClick={() => openEdit(p)} className="p-1.5 text-[#2b2622]/40 hover:text-[#B67D35] hover:bg-[#B67D35]/10 rounded-lg transition-colors"><Pencil size={15} /></button>                    <button onClick={() => handleDelete(p)} className="p-1.5 text-[#2b2622]/40 hover:text-red-500 hover:bg-red-50 rounded-lg transition-colors"><Trash2 size={15} /></button>                  </div>                </div>                <h3 className="font-display font-semibold text-[#2b2622]">{p.name}</h3>                <p className="text-xs text-[#B67D35] font-medium mt-1">{p.specialty}</p>                <div className="flex items-center gap-1 mt-3">                  <Star size={13} className="fill-[#B67D35] text-[#B67D35]" />                  <span className="text-xs text-[#2b2622]/70">{Number(p.rating || 5).toFixed(1)} ({p.reviews || 0})</span>                </div>              </div>            ))}          </div>        </div>      </div>      <Dialog open={open} onOpenChange={setOpen}>        <DialogContent className="sm:max-w-md">          <DialogHeader><DialogTitle>{editing ? 'Editar Profissional' : 'Nova Profissional'}</DialogTitle></DialogHeader>          <div className="space-y-3 py-2">            <Input label="Nome" value={form.name} onChange={(v) => setForm({ ...form, name: v })} />            <Input label="Especialidade" value={form.specialty} onChange={(v) => setForm({ ...form, specialty: v })} />            <div className="grid grid-cols-2 gap-3">              <Input label="Avaliação (0-5)" type="number" value={form.rating} onChange={(v) => setForm({ ...form, rating: v })} />              <Input label="Nº de reviews" type="number" value={form.reviews} onChange={(v) => setForm({ ...form, reviews: v })} />            </div>            <Input label="Iniciais (opcional)" value={form.initials} onChange={(v) => setForm({ ...form, initials: v })} />          </div>          <DialogFooter>            <Button variant="outline" onClick={() => setOpen(false)}>Cancelar</Button>            <Button onClick={handleSave} className="bg-[#B67D35] hover:bg-[#9c6829]">{editing ? 'Salvar' : 'Cadastrar'}</Button>          </DialogFooter>        </DialogContent>      </Dialog>    </div>  );}function Input({ label, value, onChange, type = 'text' }) {  return (    <div>      <label className="text-xs uppercase tracking-widest text-[#2b2622]/50 font-medium mb-2 block">{label}</label>      <input type={type} value={value} onChange={(e) => onChange(e.target.value)} className="w-full px-4 py-2.5 bg-white border border-[#E0DCD6] rounded-xl text-sm text-[#2b2622] focus:outline-none focus:border-[#B67D35]" />    </div>  );}