import { jsPDF } from 'jspdf';export function generateVoucher(appt) {  const doc = new jsPDF({ unit: 'mm', format: 'a4' });  const W = 210;  // Header band  doc.setFillColor(182, 125, 53);  doc.rect(0, 0, W, 38, 'F');  doc.setTextColor(255, 255, 255);  doc.setFont('helvetica', 'bold');  doc.setFontSize(20);  doc.text('Beleza & Bem-Estar', W / 2, 18, { align: 'center' });  doc.setFont('helvetica', 'normal');  doc.setFontSize(10);  doc.text('Comprovante de Agendamento', W / 2, 28, { align: 'center' });  // Body  doc.setTextColor(43, 38, 34);  doc.setFontSize(13);  doc.setFont('helvetica', 'bold');  doc.text('Detalhes do agendamento', 20, 56);  const row = (label, value, y) => {    doc.setFont('helvetica', 'normal');    doc.setFontSize(9);    doc.setTextColor(120, 120, 120);    doc.text(label, 20, y);    doc.setFont('helvetica', 'bold');    doc.setFontSize(11);    doc.setTextColor(43, 38, 34);    doc.text(String(value || '—'), 20, y + 6);  };  const fmtDate = (d) => {    if (!d) return '—';    return new Date(d + 'T00:00').toLocaleDateString('pt-BR', { day: '2-digit', month: 'long', year: 'numeric' });  };  const payLabel = appt.payment_method === 'pix' ? 'PIX' : appt.payment_method === 'credit' ? 'Cartao de Credito' : 'Cartao de Debito';  row('Cliente', appt.client_name, 68);  row('Servico', appt.service_name, 84);  row('Profissional', appt.professional_name, 100);  row('Data', fmtDate(appt.date), 116);  row('Horario', appt.time, 132);  row('Forma de pagamento', payLabel, 148);  // Total box  doc.setFillColor(245, 239, 230);  doc.roundedRect(20, 162, 170, 22, 3, 3, 'F');  doc.setFont('helvetica', 'normal');  doc.setFontSize(11);  doc.setTextColor(120, 120, 120);  doc.text('Total', 25, 173);  doc.setFont('helvetica', 'bold');  doc.setFontSize(15);  doc.setTextColor(182, 125, 53);  doc.text(`R$ ${Number(appt.total_price || 0).toFixed(2).replace('.', ',')}`, 185, 174, { align: 'right' });  // Footer  doc.setFont('helvetica', 'normal');  doc.setFontSize(9);  doc.setTextColor(150, 150, 150);  doc.text('Apresente este comprovante no dia do atendimento. Pagamento presencial.', W / 2, 200, { align: 'center' });  doc.text('Av. Paulista, 1500 - Sao Paulo  |  (11) 4002-8922', W / 2, 207, { align: 'center' });  const slug = (appt.client_name || 'agendamento').toLowerCase().replace(/\s+/g, '-');  doc.save(`comprovante-${slug}.pdf`);}